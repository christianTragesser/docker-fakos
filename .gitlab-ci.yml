image: christiantragesser/dind-ci

variables:
  DOCKER_DRIVER: overlay2

services:
  - name: docker:dind

stages:
  - test

unit_tests:
  stage: test
  tags:
    - docker

  script:
    - bash -C $CI_PROJECT_DIR/fakos_ci.sh